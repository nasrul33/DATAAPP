{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "main-capability",
  "description": "Minimum core permissions for the Teratai main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
